{
  "name": "estimador-tch-casur-pwa",
  "version": "2.5.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test tests/*.test.mjs",
    "validate": "node tests/validate-static.mjs && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  }
}
